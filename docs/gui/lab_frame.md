# `gui/lab_frame.py`

## الدور

واجهة **المختبر**: تسجيل أهداف مختبر مصرّح بها، عرض القائمة، ربما تشغيل سير عمل التحقق عبر `LabManager`.

## التكامل

يستلم `LabManager` من التطبيق؛ يستخدم `validate_target` ضمنيًا عند التسجيل.
