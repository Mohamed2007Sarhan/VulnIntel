# `database/__init__.py`

## المسار

`database/__init__.py`

## الدور

يجعل `database/` حزمة Python قابلة للاستيراد (`from database.models import init_database`, `from database.db_manager import DatabaseManager`).
