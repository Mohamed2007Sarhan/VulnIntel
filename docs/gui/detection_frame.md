# `gui/detection_frame.py`

## الدور

عرض **قواعد الاكتشاف** المولَّدة (Sigma / YARA / IOC) المسجَّلة في `detections` و/أو مسارات الملفات المحفوظة.

## الاستخدام

مراجعة ما أنشأه الـ pipeline تلقائيًا تحت `data/detections/`.
